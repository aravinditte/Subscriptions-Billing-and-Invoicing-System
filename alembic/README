This directory contains Alembic migration scripts.

Migrations are used to manage database schema changes
in a controlled, versioned manner.

Commands:
- alembic revision --autogenerate -m "message"
- alembic upgrade head
